# Configuração do RabbitMQ para a aplicação Order Management API.
#
# Carrega automaticamente, na inicialização do broker, as definições
# declaradas em definitions.json (fila "orders_created_queue" já criada
# antes mesmo da aplicação publicar a primeira mensagem).

management.load_definitions = /etc/rabbitmq/definitions.json
